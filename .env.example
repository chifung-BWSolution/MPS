# =============================================================================
# MPS — Marketing Project System
# Project: https://kwcevjcmdjadhrygjyfp.supabase.co
# =============================================================================
# Copy to .env.local for local Vite, OR set secrets in Cursor Cloud Agents.
#
# This repo must NOT use Furniture / PMS / Master Supabase credentials.
# Those belong to other apps. Keep them in their own Cloud Environments.

# --- Preferred for Cloud Agents (avoids clobbering other apps' VITE_* secrets) ---
MPS_URL=https://kwcevjcmdjadhrygjyfp.supabase.co
MPS_ANON=your_mps_anon_public_key
# Optional — agent/server only; never prefix with VITE_ (do not ship to browser)
# MPS_SERVICE=your_mps_service_role_key

# --- Used by the Vite app (auto-filled from MPS_* via scripts/sync-mps-env.mjs) ---
VITE_SUPABASE_URL=https://kwcevjcmdjadhrygjyfp.supabase.co
VITE_SUPABASE_ANON_KEY=your_mps_anon_public_key
